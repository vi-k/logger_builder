> **Состояние на 2026-08-17:** подготовлено — коммит `Release 0.6.1`, тег
> `v0.6.1`, всё запушено; **публикация за владельцем**.
> **Что это:** что вошло в 0.6.1, чем проверено и что осталось сделать.
> **Связанные записи:** `2026-08-17[2]-readme-ru-report.md`,
> `2026-08-17[3]-readme-claims-report.md`.

# Релиз 0.6.1

## Что вошло

Только документация. `lib/` не тронут ни одной строкой, публичный контракт
не менялся — поэтому патч, а не минор (`docs/conventions.md` §6).

- **Три утверждения README, которые никто не мерил, проверены и
  переписаны** — `processLog` замыканием против метода, «замыкания во всех
  случаях», `print` в stdout на web. Разбор и числа —
  в `2026-08-17[3]-readme-claims-report.md`.
- **`benchmarks.dart` получил три секции**, которыми эти числа и добыты.
  Файл едет в архиве, так что читатель может перемерить у себя.
- **`withAddedName` в примере переименован в `child`.** README перестал
  учить `withAddedName` ещё в 0.6.0 — такого имени нет ни в одном
  публичном API, — но пример, на который тот же раздел ссылается,
  по-прежнему определял старое имя. Тестовая копия того же демо-логгера
  переименована заодно, чтобы в репозитории было одно имя на одно понятие.

Чего в релизе **нет**: `README.ru.md`. Перевод в архив не попадает
(`.pubignore`), и это первый релиз, который его не везёт, — так и
задумано.

Заодно 0.6.1 — первый релиз, который довозит до pub.dev правки README,
сделанные после 0.6.0. До этого они лежали только в репозитории.

## Проверки

| Проверка | Результат |
|---|---|
| `dart analyze --fatal-infos` | No issues found! |
| `dart test` | 212/212 зелёные |
| `dart format --output=none --set-exit-if-changed .` | 45 файлов, 0 изменено |
| `dart pub publish --dry-run` | `logger_builder 0.6.1`, архив 66 КБ |
| состав архива | `README.ru.md` отсутствует, `benchmarks.dart` на месте |
| пример `hierarchical_logger.dart` после переименования | запускается, exit 0 |
| `dart doc` | Found 0 warnings and 0 errors |
| CI на релизном коммите | `verify` и `oldest-supported` — оба success |

Единственное предупреждение dry-run до релизного коммита — «2 checked-in
files are modified in git» про `CHANGELOG.md` и `pubspec.yaml`; после
коммита его нет.

Архив вырос с 63 до 66 КБ — это подросший `benchmarks.dart` и правки
README.

## Что осталось владельцу

Публикацию делает владелец руками (`AGENTS.md` §5, `docs/conventions.md`
§7 пункт 7):

```sh
dart pub publish
```

Всё, что до этого шага, сделано: проверки зелёные, `CHANGELOG.md`
дополнен, версия в `pubspec.yaml` поднята, README и перевод
синхронизированы, коммит `Release 0.6.1` и тег `v0.6.1` запушены.

В 0.6.0 публикацию по прямому указанию выполнил агент — это было разовое
отступление, и правило с тех пор не менялось.
