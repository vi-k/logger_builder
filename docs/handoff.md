# Текущее состояние проекта

> Обновлено: 2026-08-17

Только текущее состояние. История — в `docs/records/`, пожелания
владельца — в `docs/backlog.md`, устройство пакета — в
`docs/architecture.md`, правила работы — в `AGENTS.md`.

## Где мы

Второе независимое ревью разобрано целиком, 0.6.0 и 0.6.1 опубликованы.
**0.6.2 открыт и закоммичен — обычным коммитом `b73f818`, не релизным.**
Дерево чистое, запушено, CI зелёный. Подробности — «0.6.2: открыт,
не выпущен».

**0.6.2: открыт, не выпущен.** Патч, только документация и пример, `lib/`
не тронут. Пункты 1–5 чеклиста `docs/conventions.md` §7 пройдены, пункт 6
(связка «коммит — тег — публикация») ждёт **отдельного** запроса
владельца: сообщение, которым 0.6.2 открывался, содержало ещё две задачи,
поэтому ни одна часть связки не тронута. Подробности —
`docs/records/2026-08-17[5]-0.6.2-release-report.md`.

Что в `git log` **нет** и быть не должно, пока связка не разрешена: коммита
`Release 0.6.2` и тега `v0.6.2`. Открытие версии уехало обычным коммитом
с префиксом, а заголовок в `CHANGELOG.md` помечен `(unreleased)` — той же
формой, что стояла у 0.6.0 и 0.5.1 до публикации. Снять пометку —
работа релизного коммита: у `Release 0.6.0` вся правка `CHANGELOG.md`
в этом и состояла.

Что вошло. `ansi_escape_codes` в примере поднят с `^3.0.2` до `^4.0.1`.
Мажор объёмный (`Match` → `Piece`, `Link` → `OscLink`, `rgb`/`gray` →
`rgb256`/`gray256`, 530 топ-левел стилей уехали в константы класса
`Styles`), но код примера не задел ничего из этого: `dart analyze` был чист
до единой правки. Порог SDK у 4.0.1 тот же `^3.6.0`, поэтому комментарий
про пол зависимостей в pubspec примера остался верным. Правки по существу
две: `console.dart:57` считает ширину строки геттером
`lengthWithoutEscapeCodes` вместо `ansiRemoveEscapeCodes().length` (в 3.0.2
геттера не было), а пять сниппетов README зовут `Styles.red(...)` вместо
исчезнувшего топ-левел `red(...)` — в `README.md` и `README.ru.md`
одинаково, как требует `AGENTS.md` §2. Один из сниппетов при этом
переформатирован: `Styles.` длиннее на семь символов, и строка перестала
влезать в 80 колонок.

Проверки: `dart analyze` (корень, вместе с `example/`) — 0 issues,
`dart test` — 212/212, `dart format` — чисто, `dart doc` — 0 предупреждений
и 0 ошибок, все девять примеров запускаются с exit 0, `dart compile js`
и `dart compile wasm` собираются, `dart pub publish --dry-run` —
`logger_builder 0.6.2`, архив 67 КБ, 0 предупреждений (прогнан после
коммита; до него было одно — «5 checked-in files are modified in git»,
то есть само отсутствие коммита). `README.ru.md` в архив не едет,
`pubspec.lock` примера в git не ходит (`.gitignore`).

Запись ревью — `docs/records/2026-08-17[1]-0.6.0-project-review.md`,
проведено на коммите `8a35e82` по уже исправленному коду: 1 Critical,
5 High, 22 Medium, 19 Low. План работ с приоритетами — в конце той же
записи, вердикты по закрытым пунктам стоят отдельными абзацами внутри
самих находок.

**Все находки закрыты** — пять волн, вердикт стоит в конце каждой находки.
Закрыт и последний пункт плана, не бывший находкой: инвариант «флаги связи
работают как маркеры обхода и тем ломают циклы в графе саблогеров» описан
в коде и в §7 `architecture.md` и закреплён тестами.

**0.6.0 выпущен и опубликован.** Коммит `Release 0.6.0` (`c2a80b2`), тег
`v0.6.0`, CI зелёный на обоих job'ах, на pub.dev — 0.6.0 (2026-08-17 08:47
UTC, архив 63 КБ). Проверено установкой со стороны: чистый Dart-потребитель
с `logger_builder: ^0.6.0` собирается и печатает лог, Flutter 3.29 разрешает
пакет с `meta 1.16.0`.

Публикацию по прямому указанию владельца выполнил агент. Тогда это было
разовым отступлением от правила «публикует только владелец»; **правило
с тех пор изменено**. Теперь `dart pub publish` агенту разрешён, но лишь
по явному разрешению и лишь отдельной задачей: если в сообщении владельца
есть что-то ещё, кроме публикации, публиковать нельзя. Целиком —
`AGENTS.md` §5, «Публикация на pub.dev».

**0.6.1 выпущен и опубликован.** Коммит `Release 0.6.1` (`b88bb7b`), тег
`v0.6.1`, на pub.dev — 0.6.1 (2026-08-17 12:31 UTC, архив 66 КБ),
проверено запросом к API. Чеклист `docs/conventions.md` §7 пройден
целиком. Релиз чисто документационный: `lib/` не тронут, поэтому патч,
а не минор. Содержимое — три проверенных утверждения README, три новые
секции `benchmarks.dart` и переименование `withAddedName` → `child`
в примере и в тестовом демо-логгере. Подробности —
`docs/records/2026-08-17[4]-0.6.1-release-report.md`.

Публиковал агент — по отдельному запросу владельца, как теперь и
предписывает `AGENTS.md` §5: разрешение пришло сообщением, в котором
не было ничего, кроме просьбы опубликовать. Два предыдущих сообщения,
где просьба соседствовала с другой работой, закончились отказом — так
правило и задумано.

**У README появился русский перевод** — `README.ru.md`, коммиты `75ddc17`
и `1f47c3b`. Это рабочая копия владельца: живёт только в репозитории,
вписана в `.pubignore`, в архив pub.dev не попадает (проверено составом
архива), и `README.md` на неё **не ссылается** — указатель, который я
сначала добавил, владелец велел убрать, запрет записан в `AGENTS.md` §2.
Сам перевод в архив не попадает ни в каком виде.
Актуальность держится не на добрых намерениях: `AGENTS.md` §2 требует
править перевод **тем же коммитом**, что и оригинал, потому что CI за
переводом не следит и отставший перевод не падает, а молча врёт. Как
переводить — `docs/conventions.md` §1; в чеклист релиза (§7, пункт 4)
добавлена сверка перевода. Подробности —
`docs/records/2026-08-17[2]-readme-ru-report.md`.

**Три утверждения README проверены и поправлены** — коммит `b60e6b8`,
пункты бэклога закрыты, перевод уехал тем же коммитом (первая проверка
правила выше). Кратко: `processLog` замыканием против метода — разница
меньше 2 нс, и знак её меняется между AOT и JIT, так что вывод README был
верен, а обоснование нет: замыкание создаётся раз на включение уровня,
а не на вызов (`custom_level_logger.dart:339`). «Замыкания во всех
случаях» — подтвердилось числами: 3 нс поверх ~135 нс при включённом
уровне против 41 → 4 нс при выключенном. «`print` always writes to
stdout» — на web неверно: и `dart compile js`, и `dart compile wasm` шлют
`print` в `dartPrint`/`console.log`, а сам рецепт с `dart:io` на web
собирается и падает в рантайме. Мерилось тремя секциями, добавленными
в `benchmarks.dart`. Подробности —
`docs/records/2026-08-17[3]-readme-claims-report.md`.

**Порог SDK — `^3.6.0`, `meta` — `^1.15.0`.** Решение владельца после
отката подъёма до `^3.7.0`. Смысл: заявленный порог совпадает с тем, что
реально проверяет job `oldest-supported` и с pubspec примера, а понижение
`meta` — то, что вообще делает пакет пригодным для Flutter. Flutter пинит
`meta` из своего SDK **точной** версией в старых релизах (3.24 и 3.27 — обе
1.15.0), поэтому `^1.16.0` там не разрешался, хотя Dart подходил. Пакету
ничего новее не нужно: используются только `@protected`,
`@visibleForTesting` и `@immutable`.

Первый пригодный Flutter — 3.27. Проверено исполнением: Flutter 3.29 и 3.47
разрешают пакет и `flutter analyze` пробного приложения чист; потребитель с
пином `meta 1.15.0` (как Flutter 3.24/3.27) разрешается, а раньше получал
отказ солвера.

`require_trailing_commas` работает, пока версия языка ниже 3.7. Если порог
пойдёт выше — правило придётся убрать: tall-style удаляет ровно те запятые,
которых оно требует (замерено: 43 замечания против только что
отформатированного дерева).

**Dependabot разобран.** `actions/checkout` взят напрямую до v7.0.1
(SHA-пин); мажоры `lints` в ignore, пока порог `^3.6.0`, с указанием в
комментарии, когда снять. Открытых PR нет.

## Волны правок

| Волна | Содержание | Коммит |
|---|---|---|
| 1 (P0) | `C1`, `H1`, `H3` (код), `H4`, `H5`, `M12`, `M19`, часть `L18` | `dc18db3` |
| 2 (P1) | `H2`, `M1`, `M2`, `M3`, `M16`, `M20`, `M21`, `M22` | `981ea09` |
| 3 (P1) | README: `M8`-`M11`, `M15`, часть `L17` | `b022b91` |
| 4 (P2) | `M4`-`M7`, `M13`, `M14` | `eca3042` |
| 5 (P2, P3) | `M17`, `M18`, `L1`-`L19` | `99b3d28` |
| 6 | инвариант про флаги связи (не находка) | `e42226b` |
| 7 | `unnecessary_ignore` выключен: его нет в SDK 3.6.0 | `af3bfe3` |
| 8 | вердикты приведены к форме проекта в обеих записях | `b2049e3`, `e4925d5` |
| 9 | порог SDK `^3.7.0` + переформатирование под 3.7 | `3385c58`, `6128bc8` |
| 10 | откат к `^3.6.0`, `meta` понижена до `^1.15.0` | `53de7c1` |
| 11 | Dependabot: checkout v7.0.1, ignore на мажоры `lints` | `cfc815b` |

Главное по существу:

- **`C1`** — адаптер `withParam()` реализует `Flushable`/`Closable`
  и делегирует владельцу общей очереди. Раньше `MultiPublisher.close()`
  молча терял всю очередь, докладывая об успехе.
- **`H1`** — счётная карта в `AsyncFormatterWithBufferAndParam` сравнивает
  лог по identity, как в непараметрическом двойнике.
- **`H2`, `M1`** — у `CustomLogger` появился публичный **`onError`**: единая
  точка для бросившего `transformer`, срабатывания guard'а и бросившего
  паблишера. Аддитивно — без обработчика поведение прежнее. Разрешается по
  цепочке родителей, а не копируется вниз: флага связи нет, `relink()` его
  не касается.
- **`M3`** — флаг guard'а публикации перенесён с логгера на уровневый
  логгер. Паблишер, логирующий в другой уровень того же логгера, больше не
  отвергается; цикл по-прежнему ловится.
- **`M15`** — в README появился подраздел «The full set»: таблица 2×2 со
  всеми восемью асинхронными классами, разбор `onError`/`sync`/`retryDelay`
  и блок про неограниченность очередей. До этого `AsyncFormatter*`,
  `retryDelay`, `Flushable`/`Closable`, `sync` и `isClosed` не встречались
  в README ни разу.
- **`M8`-`M11`** — `withAddedName`, которого нет в пакете, заменён на
  `child(...)`; заметка о том, что нужен не только уровень, но и паблишер;
  `StateError` после `close()` назван прямо; две семантики `flush` разведены.
- **`M4`, `M5`, `M6`** — жизненный цикл при закрытии: `close()` отменяет
  ожидающий ретрай вместо ожидания `retryDelay`, `flush()` во время закрытия
  возвращает future закрытия, а не завершённый, и появился **`onDropped`** —
  колбэк с записями, отброшенными при закрытии. До этого потеря была
  невидима: ни ошибки, ни счётчика.
- **`M7`** — правило «что достаётся новому уровню» вынесено в одну функцию,
  консультирующуюся с цепочкой родителей, и появился
  **`CustomLevelLogger.hasPublisher`**: способ отличить включённый уровень,
  который пишет в никуда, от рабочего.
- **`H5`** закрыт документированием, **`M2`** и **`M3`**-часть про
  асинхронные паблишеры — тоже (см. «Решения, которые стоит знать»).

Тестов 212 (было 170). Каждая кодовая правка проверена мутацией: откат
роняет соответствующий новый тест. Из четырёх мутаций, выживавших на момент
ревью, не осталось ни одной.

## Решения, которые стоит знать

1. **`H5`: бросивший `output` по-прежнему теряет батч — это контракт.**
   Симметрию с `format` я не делал: `format` безопасно ретраить целиком,
   потому что `output` при его отказе ещё не исполнялся, а к моменту отказа
   `output` сток мог уже принять часть батча — автоматический ретрай заменил
   бы тихую потерю тихим дублированием. Изменить это значит изменить
   контракт, а не только код.
2. **`H2`: дефолт `onError` оставлен прежним сознательно.** Тихо проглатывать
   ошибку, которую библиотека сама обнаружила, хуже, чем сообщить о ней.
   Исправлено не «перестало падать по умолчанию», а «появилось чем это
   выключить».
3. **`M2`: асинхронный паблишер защитить guard'ом нельзя в принципе** — его
   `handle` идёт после возврата из `publish`. Поэтому исправлено обещание,
   а не механика.
4. **`M7`: догадок про паблишер нового уровня я не делал.** Если корневой
   логгер настроен только пер-уровнево, новый уровень остаётся на no-op —
   выдать ему паблишер, выбранный для другого уровня, было бы выдумкой.
   Закрыта ненаблюдаемость (`hasPublisher`), а не «пусть хоть куда-то
   пишет».

## Состояние проверок

Dart SDK 3.13.0 (stable), прогнано перед этой записью:

| Проверка | Результат |
|---|---|
| `dart analyze --fatal-infos` | 0 issues |
| `dart test` | 212/212 зелёные |
| `dart format --output=none --set-exit-if-changed .` | чисто |
| `dart analyze` из чистого клона + `pub get` для `example/` | 0 issues (было exit 3 — `H3`) |
| то же на **Dart 3.7.0** (из Flutter 3.29) | 0 issues, 212/212 |
| Flutter 3.29 и 3.47: `pub get` + `flutter analyze` пробного приложения | чисто |
| `dart pub publish --dry-run` | `logger_builder 0.6.2`, архив 67 КБ, **0 предупреждений** |
| все девять примеров | запускаются, exit 0 |
| `dart compile js` / `dart compile wasm` | собираются |
| CI на `HEAD` | оба job'а зелёные (`verify`, `oldest-supported`) |
| `dart doc` | Found 0 warnings and 0 errors |

Строки про Flutter и про чистый клон — с 0.6.1: их с тех пор не гоняли,
а тронуто было только `example/` и README. Остальное прогнано на 0.6.2,
dry-run — уже после коммита, на чистом дереве.

`dart doc` — 0 предупреждений, 0 ошибок. Раньше это требование держалось
на одной строке здесь и легко терялось; теперь оно в чеклисте релиза,
`docs/conventions.md` §7 пункт 1.

## Открытые вопросы

1. **Порог инструментария держит `unnecessary_ignore` выключенным.** Job
   `oldest-supported` пинит SDK 3.6.0, где такого правила нет, а неизвестное
   имя правила там же становится `undefined_lint` и роняет job. Включить —
   когда поднимется порог (см. пункт 3). Это единственное, что осталось от
   первого прогона CI: `verify` прошёл целиком, включая все четыре новых
   шага (архив, примеры, js, wasm), `oldest-supported` упал только на этом
   правиле и после правки зелёный.
2. **`maxBufferSize` и политика сброса не вводились** (вердикт по `M10`
   прошлого ревью). Очереди неограниченны, это описано в dartdoc всех четырёх
   баз. Форму API стоит обсудить отдельно. Наблюдаемость потерь при закрытии
   закрыта в волне 4 через `onDropped`, но потерь **из-за переполнения**
   по-прежнему нет как понятия.
3. **`lints 6.x` заблокирован порогом.** Ему нужен `^3.8.0`, у нас `^3.6.0`,
   и job `oldest-supported` на 3.6.0 его не разрешит — оба PR Dependabot на
   `lints 6.1.0` упрутся в это. То же с `unnecessary_ignore`: проверено на
   настоящем Dart 3.7.0, что правила там ещё нет, значит и оно ждёт 3.8.
   Подъём порога — отдельное решение, и оно дороже, чем кажется: версия
   языка 3.7 переключает `dart format` на tall-style (37 файлов из 45), а
   `require_trailing_commas` придётся выключить.

4. **У волны 0.5.0 нет записи в `docs/records/`.** Что вошло, видно по
   `CHANGELOG.md` и коммитам `313e3f0..89730fb`.

## Что дальше

1. 0.6.2 подготовлен целиком и ждёт хода владельца — связку из пункта 6
   чеклиста запускает только отдельный запрос. Работы в нём не осталось.
2. Остаток бэклога — один пункт: саблогеры другого типа. Работа крупная и
   breaking (потребует пересмотра генерик-связки), начинать с обсуждения
   и design-записи, а не с кода.

## Если сессия оборвалась здесь

По находкам ревью не осталось ничего, 0.6.1 опубликован и проверен на
pub.dev. Дерево чистое: весь 0.6.2 закоммичен обычным коммитом `b73f818`,
проверки из «Где мы» прогнаны на нём, всё запушено, CI зелёный на обоих
job'ах. Публикации не было и связка не
разрешена: тега `v0.6.2` нет, `CHANGELOG.md` помечен `(unreleased)`,
на pub.dev по-прежнему 0.6.1.

Если `example/.../.dart_tool` не поднялся вместе с деревом — `dart pub get
--directory=example/logger_builder_examples`, `pubspec.lock` примера
в git не ходит.

Перед любым следующим действием, по `AGENTS.md` §1: сверь `git status
--short` и `git log --oneline -5` с этой запиской. Если расходится — верь
git и приведи этот файл в соответствие.

**Если рестарт когда-нибудь придётся на публикацию — сначала спроси
pub.dev, а не запускай `dart pub publish` заново.** Публикация
необратима, и повторный запуск на уже опубликованной версии — не «ещё
раз», а ошибка, после которой номер израсходован. Проверка:

```sh
curl -s https://pub.dev/api/packages/logger_builder | \
  python3 -c 'import json,sys; print(json.load(sys.stdin)["latest"]["version"])'
```

Версия на месте — публикация состоялась: допиши факт сюда и в запись
релиза, и ничего не запускай. Версии нет — не состоялась, и разрешение
нужно получать заново: оно даётся на одну публикацию и сгорает вместе
с сессией, см. `AGENTS.md` §5.

Чего в репозитории **нет** и не должно быть: временные пробы, которыми
проверялись находки ревью, жили в скретчпаде сессии вне репозитория и не
сохранились. Все доказательства пересказаны в вердиктах внутри записей
ревью — замеры, до/после, названия добавленных регресс-тестов и мутации,
которыми правки проверены. Воспроизводить пробы заново не нужно: то, что
они показывали, закреплено тестами в `test/`.
