# Текущее состояние проекта

> Обновлено: 2026-08-20

Только текущее состояние. История — в `docs/records/`, пожелания
владельца — в `docs/backlog.md`, устройство пакета — в
`docs/architecture.md`, правила работы — в `AGENTS.md`.

## Где мы

**2026-08-20 проведено четвёртое независимое ревью — 23 находки, из них
закрыто двадцать.** Двумя волнами: сначала три P0, потом Medium и Low.
Вердикт стоит в конце каждой закрытой находки в записи
`docs/records/2026-08-20[1]-project-review.md`, отчёты о волнах —
`docs/records/2026-08-20[2]-p0-wave-report.md` и
`docs/records/2026-08-20[3]-medium-low-wave-report.md`. Открыты три
находки — `M2`, `L6`, `L8`, — и каждая ждёт решения владельца, а не
работы. Раздел «Четвёртое независимое ревью: двадцать находок из
двадцати трёх закрыты».

Второе независимое ревью разобрано целиком, 0.6.0 и 0.6.1 опубликованы.
**0.6.2 и 0.7.0 открыты и закоммичены — обычными коммитами, не
релизными.** Подробности — «0.6.2: открыт, не выпущен» и «0.7.0: пин
паблишера сделан».

Дерево чистое и **запушено**, локальных коммитов впереди нет. Последняя
работа — умолчание `maxQueueSize` поднято до 100 000 (`2e25f36`), до неё
дефолтный `onDropped` (`ec4ba44`, `7a9e069`), до неё граница очереди
(`19e6b52`..`64e5241`), до неё сведение обвязки фасадов (`4ce28ac`,
`1150d6f`). Разделы «Умолчание очереди — 100 000», «Дефолтный
`onDropped` говорит», «Граница очереди введена» и «Обвязка фасадов
сведена, обе пары». CI
зелёный на всех четырёх job'ах, включая проверку тегов pub.dev через
pana.
Это закрывает то, что двадцать один коммит висело непроверенным:
`oldest-supported` гоняет настоящий Dart 3.6.0, то есть заявленный пол
подтверждён и для кода волны по ревью.

**0.6.2: открыт, не выпущен.** Патч, только документация и пример, `lib/`
не тронут. Пункты 1–5 чеклиста `docs/conventions.md` §7 пройдены, пункт 6
(связка «коммит — тег — публикация») ждёт **отдельного** запроса
владельца: сообщение, которым 0.6.2 открывался, содержало ещё две задачи,
поэтому ни одна часть связки не тронута. Подробности —
`docs/records/2026-08-17[5]-0.6.2-release-report.md`.

Что в `git log` **нет** и быть не должно, пока связка не разрешена: коммита
`Release 0.6.2` и тега `v0.6.2`. Открытие версии уехало обычным коммитом
с префиксом, а заголовок в `CHANGELOG.md` помечен `(unreleased)` — той же
формой, что стояла у 0.6.0 и 0.5.1 до публикации. Снять пометку —
работа релизного коммита: у `Release 0.6.0` вся правка `CHANGELOG.md`
в этом и состояла.

Что вошло. `ansi_escape_codes` в примере поднят с `^3.0.2` до `^4.0.1`.
Мажор объёмный (`Match` → `Piece`, `Link` → `OscLink`, `rgb`/`gray` →
`rgb256`/`gray256`, 530 топ-левел стилей уехали в константы класса
`Styles`), но код примера не задел ничего из этого: `dart analyze` был чист
до единой правки. Порог SDK у 4.0.1 тот же `^3.6.0`, поэтому комментарий
про пол зависимостей в pubspec примера остался верным. Правки по существу
две: `console.dart:57` считает ширину строки геттером
`lengthWithoutEscapeCodes` вместо `ansiRemoveEscapeCodes().length` (в 3.0.2
геттера не было), а пять сниппетов README зовут `Styles.red(...)` вместо
исчезнувшего топ-левел `red(...)` — в `README.md` и `README.ru.md`
одинаково, как требует `AGENTS.md` §2. Один из сниппетов при этом
переформатирован: `Styles.` длиннее на семь символов, и строка перестала
влезать в 80 колонок.

Проверки: `dart analyze` (корень, вместе с `example/`) — 0 issues,
`dart test` — 212/212, `dart format` — чисто, `dart doc` — 0 предупреждений
и 0 ошибок, все девять примеров запускаются с exit 0, `dart compile js`
и `dart compile wasm` собираются, `dart pub publish --dry-run` —
`logger_builder 0.6.2`, архив 67 КБ, 0 предупреждений (прогнан после
коммита; до него было одно — «5 checked-in files are modified in git»,
то есть само отсутствие коммита). `README.ru.md` в архив не едет,
`pubspec.lock` примера в git не ходит (`.gitignore`).

Запись ревью — `docs/records/2026-08-17[1]-0.6.0-project-review.md`,
проведено на коммите `8a35e82` по уже исправленному коду: 1 Critical,
5 High, 22 Medium, 19 Low. План работ с приоритетами — в конце той же
записи, вердикты по закрытым пунктам стоят отдельными абзацами внутри
самих находок.

**Все находки закрыты** — пять волн, вердикт стоит в конце каждой находки.
Закрыт и последний пункт плана, не бывший находкой: инвариант «флаги связи
работают как маркеры обхода и тем ломают циклы в графе саблогеров» описан
в коде и в §7 `architecture.md` и закреплён тестами.

**0.6.0 выпущен и опубликован.** Коммит `Release 0.6.0` (`c2a80b2`), тег
`v0.6.0`, CI зелёный на обоих job'ах, на pub.dev — 0.6.0 (2026-08-17 08:47
UTC, архив 63 КБ). Проверено установкой со стороны: чистый Dart-потребитель
с `logger_builder: ^0.6.0` собирается и печатает лог, Flutter 3.29 разрешает
пакет с `meta 1.16.0`.

Публикацию по прямому указанию владельца выполнил агент. Тогда это было
разовым отступлением от правила «публикует только владелец»; **правило
с тех пор изменено**. Теперь `dart pub publish` агенту разрешён, но лишь
по явному разрешению и лишь отдельной задачей: если в сообщении владельца
есть что-то ещё, кроме публикации, публиковать нельзя. Целиком —
`AGENTS.md` §5, «Публикация на pub.dev».

**0.6.1 выпущен и опубликован.** Коммит `Release 0.6.1` (`b88bb7b`), тег
`v0.6.1`, на pub.dev — 0.6.1 (2026-08-17 12:31 UTC, архив 66 КБ),
проверено запросом к API. Чеклист `docs/conventions.md` §7 пройден
целиком. Релиз чисто документационный: `lib/` не тронут, поэтому патч,
а не минор. Содержимое — три проверенных утверждения README, три новые
секции `benchmarks.dart` и переименование `withAddedName` → `child`
в примере и в тестовом демо-логгере. Подробности —
`docs/records/2026-08-17[4]-0.6.1-release-report.md`.

Публиковал агент — по отдельному запросу владельца, как теперь и
предписывает `AGENTS.md` §5: разрешение пришло сообщением, в котором
не было ничего, кроме просьбы опубликовать. Два предыдущих сообщения,
где просьба соседствовала с другой работой, закончились отказом — так
правило и задумано.

**У README появился русский перевод** — `README.ru.md`, коммиты `75ddc17`
и `1f47c3b`. Это рабочая копия владельца: живёт только в репозитории,
вписана в `.pubignore`, в архив pub.dev не попадает (проверено составом
архива), и `README.md` на неё **не ссылается** — указатель, который я
сначала добавил, владелец велел убрать, запрет записан в `AGENTS.md` §2.
Сам перевод в архив не попадает ни в каком виде.
Актуальность держится не на добрых намерениях: `AGENTS.md` §2 требует
править перевод **тем же коммитом**, что и оригинал, потому что CI за
переводом не следит и отставший перевод не падает, а молча врёт. Как
переводить — `docs/conventions.md` §1; в чеклист релиза (§7, пункт 4)
добавлена сверка перевода. Подробности —
`docs/records/2026-08-17[2]-readme-ru-report.md`.

**Три утверждения README проверены и поправлены** — коммит `b60e6b8`,
пункты бэклога закрыты, перевод уехал тем же коммитом (первая проверка
правила выше). Кратко: `processLog` замыканием против метода — разница
меньше 2 нс, и знак её меняется между AOT и JIT, так что вывод README был
верен, а обоснование нет: замыкание создаётся раз на включение уровня,
а не на вызов (`custom_level_logger.dart:339`). «Замыкания во всех
случаях» — подтвердилось числами: 3 нс поверх ~135 нс при включённом
уровне против 41 → 4 нс при выключенном. «`print` always writes to
stdout» — на web неверно: и `dart compile js`, и `dart compile wasm` шлют
`print` в `dartPrint`/`console.log`, а сам рецепт с `dart:io` на web
собирается и падает в рантайме. Мерилось тремя секциями, добавленными
в `benchmarks.dart`. Подробности —
`docs/records/2026-08-17[3]-readme-claims-report.md`.

**Порог SDK — `^3.6.0`, `meta` — `^1.15.0`.** Решение владельца после
отката подъёма до `^3.7.0`. Смысл: заявленный порог совпадает с тем, что
реально проверяет job `oldest-supported` и с pubspec примера, а понижение
`meta` — то, что вообще делает пакет пригодным для Flutter. Flutter пинит
`meta` из своего SDK **точной** версией в старых релизах (3.24 и 3.27 — обе
1.15.0), поэтому `^1.16.0` там не разрешался, хотя Dart подходил. Пакету
ничего новее не нужно: используются только `@protected`,
`@visibleForTesting` и `@immutable`.

Первый пригодный Flutter — 3.27. Проверено исполнением: Flutter 3.29 и 3.47
разрешают пакет и `flutter analyze` пробного приложения чист; потребитель с
пином `meta 1.15.0` (как Flutter 3.24/3.27) разрешается, а раньше получал
отказ солвера.

`require_trailing_commas` работает, пока версия языка ниже 3.7. Если порог
пойдёт выше — правило придётся убрать: tall-style удаляет ровно те запятые,
которых оно требует (замерено: 43 замечания против только что
отформатированного дерева).

**Dependabot разобран.** `actions/checkout` взят напрямую до v7.0.1
(SHA-пин); мажоры `lints` в ignore, пока порог `^3.6.0`, с указанием в
комментарии, когда снять. Открытых PR нет.

## Волна по независимому ревью: код закончен

Третье независимое ревью — запись
`docs/records/2026-08-19[2]-project-review.md`, проведено на `eca63e3`.
Находки: 3 Critical/High, кладущие приложение потребителя, 4 High,
17 Medium, 22 Low. Владелец взял в работу **P0+P1, только код**;
документация, CI и дедупликация — отдельной волной.

| Находка | Что было | Коммит |
|---|---|---|
| `C3` | `onError`, который логирует, рекурсировал до Stack Overflow | `4a05fb1` |
| `H1` | `onError` резолвился обходом цепочки на каждом логе | `534f342` |
| `H2` | `relink()` оставлял чужой паблишер при пер-уровневом родителе | `3214f3f` |
| `M4` | `TransformPublisher` ронял вызов лога вместо своего `onError` | `a51ae52` |
| `M1` | `flush()` во время `close()` врал в шести классах из восьми | `deba992` |
| `C1`, `C2` | бесконечный ретрай: шторм, невыходящий процесс, висящий `flush` | `d0d4f62` |

Вердикты стоят внутри самих находок в записи ревью. Попутно закрыты
`L10` (висящий `flush`) и `M3` (ложное обещание в `CHANGELOG.md` про
поломку сборки у подкласса).

**`C1`/`C2` — ломающее изменение и новый публичный API.** У четырёх
буферизованных классов появился `maxRetries` (дефолт 100), `retryDelay`
удваивается на каждой попытке с потолком в 32 базы, исчерпанный бюджет
отдаёт батч в `onDropped`. Безграничного значения нет намеренно: старое
поведение и есть дефект. Форму выбрал владелец из четырёх вариантов.
Замер: 242 820 вызовов `format` за полсекунды превратились в 101.
Всё это уложено в `CHANGELOG.md` 0.7.0; версия не менялась, она открыта
и не выпущена.

Тестов 274 (было 226). Появился `test/lifecycle_contract_test.dart` —
контракт `Flushable`/`Closable` назван один раз и прогоняется по всем
восьми асинхронным классам и обеим обёрткам; это же закрывает
организационную причину половины находок, потому что тесты
у зеркальных классов писались только для одного близнеца из пары.

Каждая правка проверена мутацией; там, где мутация выживала, дописан
тест. Так появились пять тестов, которых иначе бы не было: «обработчик
зовётся снова на следующем сбое», «relink спускает уровень родителя
саблогеру, у которого он есть», «relink не подхватывает пин из цикла
обратно в себя», «бюджет ретраев восстанавливается после удачного
батча» и проводка трёх параметров в `*BufferAndParam`.

## Вторая волна по ревью: тесты и CI

Взята по рекомендации, после первой. Закрыты `H3`, `M13`, `M14`, `M15`,
`M17`, `L17`-`L20` и все восемь мутантов, выживавших в наборе.

**Тесты** (`3fed99b`), 274 → 283. Восемь мутаций, переживавших набор при
96.6 % строчного покрытия, теперь роняют тесты. Три из них охраняют
инварианты, которые код называет измеренными и которые не видны
в поведении, только в счётчике: ранний выход `_toggle` (он же сохраняет
identity функции, о хранении которой предупреждает дартдок) и удвоение
порога прунинга вместе с учётом `_prunedAt` — для него добавлена фикстура
`PruneCountingLogger`. Две — конструктор `CustomLog`: вывод стека из
брошенного `Error` и захват `Zone.current`; группа тестировала только
`copy`, а `copy` по контракту присваивает буквально. Две — обещания,
записанные в комментариях: слушатель остаётся в зоне конструктора
и после `flush`, и `onDropped` получает копию, а не живой retry-буфер
(проверяются оба пути дропа — первая версия покрывала один). Последняя —
`M17`: guard реентрантности `TransformPublisher` не исполнялся вообще
ни одним тестом.

**CI** (`139c1dd`). Шаг, охранявший web-бейджи, не мог этого делать:
`dart compile js`/`wasm` принимают `import 'dart:io'` и падают только
в рантайме, а компилируемый пример его уже содержит через `console.dart`.
Замер на копии с `dart:io` в экспортируемом `levels.dart`: обе компиляции
вышли с нулём, pana потеряла все три тега и осталась на 160/160 — поэтому
новый шаг проверяет теги, а не счёт. Плюс `dart doc` в CI, job
`oldest-deps` (downgrade резолвит ровно `meta 1.15.0`), job `beta`
с `continue-on-error`, `find` вместо двух глобов с проверкой непустого
вывода примера, `cancel-in-progress` только для PR, включён
`avoid_dynamic_calls` (ноль попаданий и на 3.13, и на настоящем 3.6.0),
и `dart analyze --fatal-infos` в чеклистах `AGENTS.md` §7
и `docs/conventions.md` §4 — до сих пор они предписывали более слабую
команду, чем гоняет CI.

## Третья волна по ревью: документация

Закрыты `M2`, `M6`, `M7`, `M8`, `L11`-`L14`, одним коммитом `81b4ea3`
вместе с переводом — как требует `AGENTS.md` §2.

Главное: **`CustomLogger.onError` не был описан в README вообще.** Все
шесть упоминаний `onError` там относились к паблишерам, а единой точки,
куда стекаются бросивший трансформер, бросивший паблишер и срабатывание
страховки, не было. Теперь есть раздел «Errors on the publish path» /
«Ошибки на пути публикации», и в нём же предупреждение, которого не было
и в дартдоке: обработчик не должен логировать через свой логгер
(находка `C3`). Второе: `onDropped` не встречался ни разу, а список над
ним объявлял три аргумента исчерпывающими — у буферизованных их пять,
включая новый `maxRetries`, который до этой волны жил только
в `CHANGELOG.md` и дартдоке.

Четыре утверждения, которые код опровергал, заменены на верные:
`close()` не ждёт взведённый ретрай (замер: `retryDelay` 5 с, `close()`
1 мс); страховок две, и охват у них разный; наследуется четыре вещи,
а не три; и абзац `architecture.md` про «оба семейства возвращают
завершённый future» — после сведения контракта неверный уже для всех.
Плюс вид `app | auth | login` в первом же сниппете недостижим тем
логгером, на который README ссылался.

Паритет перевода проверен структурно: 28 заголовков совпадающих уровней,
63 блока кода с идентичным кодом (переведённые комментарии не в счёт),
6 админошенов в том же порядке, те же 37 внешних ссылок.

## Четвёртая волна по ревью: бенчмарки

Закрыты `M10`, `M11`, `M12` (кроме одного пункта), `L15`, `L16` —
коммиты `df444fb`, `6f53cb1`, `3f461c6`.

**Харнесс** печатал одно число — среднее по лучшей половине — и ничего,
чем его оценить. Теперь медиана плюс min, max, mean и sd. Пустой цикл
измеряется отдельно и печатается (0.46 нс AOT, 0.26 JIT), а не
вычитается. Шапка называет режим (AOT или JIT), версию Dart и хост.
Общий счётчик заворачивается на миллионе — он давал систематический
дрейф 2.7 %, больше эффектов, которые из этого файла цитировались.
`BenchmarkMode.best/worst` стал `Highlight.good/bad`: он выбирал цвет,
а назывался как статистика.

**Измерено то, чего не было.** Глубина 0/1/5/20: 83.2 / 83.2 / 83.6 /
83.5 нс включённый и 1.66 / 1.78 / 1.72 / 1.77 выключенный — плоско, и
это прямой сторож против `H1`. Отдельная секция с паблишером, читающим
`path`, **не** плоская: 101.7 → 152.0 нс, ~2.5 нс на уровень; ленивый
путь саблогера не резолвился ни одной секцией. Публикующий слой:
трансформер 4.7 нс, `TransformPublisher` 5.2, каждый следующий получатель
`MultiPublisher` около 48.

**Перезамер поймал два неверных числа в README** (`3f461c6`): направление
у JIT в сравнении `processLog` не воспроизводится (11.75 против 11.99 при
sd 0.25 и 0.28 — разницы нет), и «замыкание добавляет 3 нс» тоже нет —
три формы неразличимы. Рекомендация не изменилась, обоснование стало
честнее. Перевод — тем же коммитом.

**Сторож в `test/`** (`6f53cb1`): два соотношения по глубине, порог 1.5
измерен, а не выбран — с восстановленной `H1` соотношение 1.73-1.76, без
неё 0.93-1.08; 15 прогонов подряд зелёные, 3 прогона с регрессией
красные. Первая версия файла проверяла ещё и «выключенный дешевле
включённого в 10 раз» и флакнула: это свойство полезной нагрузки, а не
пакета (9 у этой фикстуры, 2 у одно-полевой, 49 у строящей строку), и
проверка убрана.

## Пятая волна: дедупликация и остаток

Закрыты `M5`, `M9`, `M16` (частично, см. ниже), `L1`-`L9`, `L21`, `L22` —
коммиты `6fbc728`, `0ed4e78`, `2fbed7f`, `959e265`.

**`M16`.** Небуферизованная пара сведена на внутренний `AsyncPipeline<E>` —
тот же приём, что `BufferedPipeline` дал буферизованной: минус 238 строк
при плюс 69. Пять копий `_reportError` (и `_reportDropped`) — к `reportTo`
поверх меньшего `guarded`. Четыре копии преамбулы обхода саблогеров —
к `_liveSubloggers()`. **Не сведено:** обвязка двух буферизованных фасадов
(~110 строк публичных полей с дартдоком, который pub.dev показывает по
классам) и тела двух буферизованных форматтеров (~60 строк). Цена второго
видна сразу: `L8` пришлось чинить и тестировать дважды. (Оба остатка
сведены позже: тела — `fd98a87`, обвязка — `4ce28ac`.)

**Пять дефектов из Low, три из них молчаливые.** `TypedLazy.value` не
мемоизировал бросающий `convert`; `output` вызывался с пустым списком,
когда `format` вернул весь батч; буферизованный паблишер брал зону при
первом `publish`, а не при создании; четыре обхода саблогеров шли по
живому списку и падали с `ConcurrentModificationError`, если саблогер
регистрировали из `processLog`; `_relinkLevel` не гасил свой флаг.

**`M5` закрыт второй ветвью, а не первой.** Валидацию родителя
в `registerSublogger` я попробовал и откатил: она делает циклический граф
непостроимым, а вместе с ним — группу тестов, которой пакет прикрывает
эти циклы. Нужен ли люк вообще — решение об устройстве, и оно за
владельцем. Дартдок теперь говорит прямо: логгер, зарегистрированный не
через `.sub`, посчитан, глух и связать его нельзя никогда.

**Три комментария оправдывали код неправдой** и исправлены: аналогия
с `late final` в `Lazy` (там всё наоборот, измерено), «no-op нельзя
опознать снаружи» (`CustomLogPublisher.noOp()` публичен), и описание
`_defaultPublisher`, которое умалчивало о втором источнике записи.

Тестов 292 (было 285). Дважды набор поймал мою собственную ошибку в правке:
поднятый вместо восстановленного флаг в `_relinkLevel` и порог сторожа,
взятый из AOT-бенчмарка вместо среды теста.

## Ревью разобрано целиком

**45 находок из 45 закрыты.** Вердикт стоит в конце каждой находки
в `docs/records/2026-08-19[2]-project-review.md`; у восьми он расходится
с находкой и говорит об этом прямо — «исправлено частично», «исправлено
правкой обещания, а не кода», «закрыто второй из предложенных ветвей»,
«исправлено защитно, теста нет и не может быть».

## Шестая волна: дочистка

`M12` и `M16` доведены — коммиты `6ebdf7f`, `fd98a87`, `4faef47`,
`f14b2b3`.

**`M12`.** Асинхронные паблишеры измерены впервые, на вопрос «за лог,
включая слив»: `AsyncPublisher` 152 нс, с `sync: true` 128,
`AsyncPublisherWithBuffer` **50**. То есть буферизация не стоит,
а экономит, втрое. Флэш после каждого лога — 1258 и 1417 нс, в 8-28 раз
дороже; ровно это дартдок называл «measurably more expensive» без
единого замера, и теперь там стоят числа. Разброс на этих строках широкий
(sd 500-600 нс на флэш-строках): слив зависит от цикла событий и
сборщика, медиана несёт порядок величины, sd печатается рядом.

**`M16`.** Тела двух буферизованных форматтеров свелись к `handleBatch`.
Обвязка двух фасадов (~110 строк) тогда **сознательно не сведена**: это
публичные поля с дартдоком, который pub.dev показывает по классам.
(Сведена позже, по решению владельца, — `4ce28ac`.)

Попутно закрыт пробел, которого в ревью не было: `EqLog` лежал
в `*AndParam`-тестах с самого написания фикстуры и не использовался —
identity-ключ у того форматтера не стерёг никто. Первая мутация, которой
я это проверял, была **недействующей**: я менял `equals`, не тронув
`hashCode`, и записи просто не попадали в одну корзину. Убивает тест
только мутация обоих.

Тестов 293.

## Обвязка фасадов сведена, обе пары

Единственный вопрос, который `docs/handoff.md` держал открытым, закрыт по
указанию владельца: обвязка обоих буферизованных фасадов уехала в
приватную базу `_BufferedFacade<E>` — `4ce28ac`. Следом, отдельным
указанием, сведена и небуферизованная пара — база `_AsyncFacade<E>`,
коммит `1150d6f`. Отчёты с постраничным разбором изменений в публичном
dartdoc: `docs/records/2026-08-19[3]-buffered-facade-merge-report.md` и
`docs/records/2026-08-19[4]-unbuffered-facade-merge-report.md`.

Коротко. Пять настроек с их дартдоком, ленивое создание очереди,
`isClosed`, `flush`, `close` были написаны дважды; теперь один раз, `E` —
то, что лежит в очереди (лог или пара `(Param, Log)`). База приватная, а
param-файл стал `part` буферного — тем же приёмом, что
`custom_level_logger.dart`. Иначе базе пришлось бы открыть очередь
публичным членом, и он появился бы на страницах pub.dev у классов, которые
пользователь наследует; проверено — `_BufferedFacade` не встречается
в `dart doc` ни разу. Два файла: 648 строк было, 557 стало.

Цена, замеренная сравнением сборок `dart doc` до и после: общий текст
говорит «entries» там, где лог-фасад говорил «logs» (`onError`,
`onDropped`, `close`), `close` не ссылается на `publish`, у членов
появилась пометка `inherited`. Параметрический `close` при этом получил
два верных абзаца, которых у него не было. Страницы `Flushable`,
`Closable` и четырёх конкретных классов не изменились.

У небуферизованной пары то же самое, плюс своя особенность: `handle`
у неё разной арности (`handle(Log)` против `handle(Param, Log)`), свести
его в один член нельзя. База просит у наследника не метод, а функцию —
геттер `_entryHandler`, который она один раз отдаёт очереди. Лог-фасад
возвращает оттуда свой `handle` напрямую, поэтому путь лога не
удлинился. Цена в дартдоке: две ссылки на `handle` (в `onError` и
`close`) стали кодом — в базе такого члена нет, и `comment_references`
это поймал сразу. Два файла: 407 строк было, 370 стало.

## Граница очереди введена

`M10` прошлого ревью закрыт целиком: у всех восьми асинхронных паблишеров
есть `maxQueueSize` — по умолчанию 100 000 принятых и ещё не обработанных
записей, `null` снимает границу. На пределе отказ получает входящий лог:
он уходит в `onDropped` и в очередь не попадает, поэтому всё принятое
по-прежнему доставляется, а `flush` и `close` значат прежнее. `onDropped`
появился и у небуферизованной четвёрки, где потерю нельзя было увидеть
никак. Дизайн — `docs/records/2026-08-19[5]`, план — `[6]`, отчёт — `[7]`.

**Замер вскрыл то, чего не было ни в находке, ни в дизайне.** Первый
прогон A/B показал «после» в восемь раз быстрее — потому что бенчмарк
перестал доставлять логи: он публикует 20 000 штук одним синхронным
циклом, а граница по умолчанию была тогда 10 000. Очередь разбирается
только на обороте цикла событий, значит плотный цикл теряет остаток при
сколь угодно живом стоке. Это записано в дартдок `maxQueueSize` обеих баз
и в оба README; харнес бенчмарка получил границу с запасом (`fix`
`b14e385`). На исправленном харнесе разница до/после меняет знак между
прогонами и лежит внутри sd. Само умолчание пересмотрено позже — раздел
ниже.

Тестов тогда стало 313 (было 293), из них 20 — в новом
`test/queue_overflow_test.dart`, включая контракт по всем восьми классам
разом.

## Дефолтный `onDropped` говорит

Пустой `onDropped` больше не значит тишину: первая потеря печатает строку
сразу, дальше окно глотает потери и печатает счёт — 5 с с удвоением до
минуты, сброс к пяти, когда шторм кончился. `onDropped: (_) {}`
заглушает. Публичная поверхность не выросла: изменилось только то, что
значит `onDropped == null`. Дизайн — `docs/records/2026-08-19[8]`,
отчёт — `[9]`; коммиты `ec4ba44`, `7a9e069`.

Печатает, а не отдаёт в зону, потому что зона в обычной Dart-программе
завершает изолят: убивать приложение за то, что оно не успевает писать
логи, — худший ответ. Таймеров нет: часы читаются в момент потери,
взведённый таймер продлевал бы жизнь процесса из-за отброшенного лога.
Это первый вывод, который пакет делает сам, — проверено, что девять
примеров не печатают ни строки.

## Умолчание очереди — 100 000

Граница очереди по умолчанию поднята с 10 000 до 100 000 записей
(`2e25f36`) — решение владельца от 2026-08-19, из трёх названных ему
вариантов (оставить, поднять, вернуть безграничное умолчание). Отчёт —
`docs/records/2026-08-19[10]-max-queue-size-default-report.md`.

Почему пересматривали: 10 000 выведено из «десяти секунд неработающего
стока», а замер показал, что для плотного синхронного цикла это
рассуждение не работает вовсе. 100 000 несёт синхронную пачку размером
с бенчмарк пакета (20 000 логов) целиком, стоит около 20 МБ на логах по
двести байт и оставляет прежнее рассуждение об аварии в силе — только
запаса в нём стало вдесятеро больше. Батч-скрипт на миллионы строк без
единого `await` этим по-прежнему не спасён, и дартдок теперь говорит об
этом прямо, вместо того чтобы предлагать читателю вывести это из замера.

Число закреплено тестом: группа `the default bound` строит все восемь
классов без единого аргумента и читает с них `maxQueueSize`. До правки
все восемь падали — мутация проверена. Три «пачечных» замера бенчмарка
лишились явного `maxQueueSize: 100000`: теперь это умолчание, и
анализатор сам сказал об этом через `avoid_redundant_argument_values`.

## Четвёртое независимое ревью: двадцать находок из двадцати трёх закрыты

Проведено 2026-08-20 на `f03e962`. Запись —
`docs/records/2026-08-20[1]-project-review.md` (`35c27d2`).
Кода ревью не трогало: оно было независимым. Привлекались субагенты
и второй движок (Codex); четыре субагента подвисли, их области закрыты
вручную.

Разобрано двумя волнами. **Волна P0** — `7261a4e`, `d469c3c`, `714755a`,
отчёт `2026-08-20[2]`. **Волна Medium и Low** — `36eb2a6`, `07f6e3c`,
`4bc0a1c`, `d32c6d7`, `2a4c128`, `8ce6233`, `c0d0119`, `763766d`,
`3c451db`, отчёт `2026-08-20[3]`. Вердикт стоит в конце каждой закрытой
находки; проверено обходом — у каждой ровно один.

Три находки P0, все три в коде, который добавила 0.7.0:

1. **H1. Дартдок четырёх публичных баз описывал удалённое поведение** —
   «очередь неограничена, политики переполнения нет, счётчика потерь
   нет», неверны все три, плюс утверждение про зону, обратное правде
   и собственному тесту пакета. **Закрыто `714755a`.** Абзац про зону не
   удалён, как предлагала находка, а перевёрнут: зона захватывается
   в конструкторе.
2. **H2. Дефолтный репортёр потерь ронял и публикацию, и `close()`** —
   пользовательский `onDropped` обёрнут в `guarded`, а сток репортёра
   нет. **Закрыто `7261a4e`:** все три вызова стока через `guarded`, окно
   сбрасывается при любом исходе, два регресс-теста.
3. **H3. `TransformPublisher.close()` не был терминален** при синхронно
   бросающем внутреннем `close()`. **Закрыто `d469c3c`:** `Future.sync`,
   как у `MultiPublisher`.

Из семнадцати закрытых Medium и Low стоит знать про четыре — остальные
пересказаны в отчёте `2026-08-20[3]`:

- **M1 и M7** были про тесты, которые выглядели стражами и не были ими.
  Снятие `guarded` вокруг `onDropped` не роняло ни одного теста ни в
  одном семействе; страж находки C1 проходил с восстановленным
  микротаск-циклом, потому что бюджет `maxRetries` обрывал цикл сам.
  Оба закрыты и проверены мутацией.
- **M4** добавил в CI прогон тестов в браузере и под wasm: до этого ни
  одна строка пакета не исполнялась на платформах, которые он
  рекламирует. Четыре теста помечены `testOn: 'vm'` — они утверждают
  состоявшуюся сборку мусора, а JS `WeakRef` чистится по политике хоста.
- **M3** зафиксировал состав архива в `.github/published_files.txt`:
  `dry-run` лишний файл не ловит — печатает и выходит с нулём.
  **Меняете состав пакета — обновляйте этот файл тем же коммитом**
  (`docs/conventions.md` §8).

**Открыты три находки, и все три — вопрос к владельцу, а не работа:**

- **M2. Дефолтный репортёр пишет в stdout приложения.** Смена канала
  меняет умолчание, и цена не та, что в находке: `stderr` живёт
  в `dart:io`, а `dart:io` в `lib/` стоит трёх web-тегов на pub.dev
  (160/160 → 130/160, проверено самим ревью). Выбор поэтому не «stdout
  или stderr», а «условный импорт», «`dart:developer.log()`» или
  «оставить `print` и назвать канал в доках».
- **L6. Версия 0.3.1 опубликована, тега `v0.3.1` в git нет.** Тег задним
  числом — правка истории релизов; `AGENTS.md` §5 обращается с тегами
  строго.
- **L8. Покрытие не измеряется.** Ввести `--coverage` и порог — политика
  проекта.

**Что в ревью проверялось и НЕ подтвердилось** — не перепроверять:
off-by-one границы очереди (при лимите N принимается ровно N), утечка
`_pending` через повторные `flush()`, рассогласование пер-уровневого пина,
нарушения FIFO при ретрае, находки без вердикта в прошлых записях
(45 из 45 на месте), структурное расхождение README и перевода, секреты,
ReDoS, запись в файлы или сеть по умолчанию. Отдельно: заявление о том,
что `performance_guard_test.dart` флейкует в 20 % прогонов, **проверено
и не подтвердилось** — 20 зелёных из 20, включая двукратную перегрузку
всех ядер; осталось лишь конструктивное замечание L11.

## Что осталось

**Из ревью 2026-08-20 открыты три находки: `M2`, `L6`, `L8`.** Ни одна
не работа — все три вопрос к владельцу, раздел выше. Остальные двадцать
закрыты. Всё, что ниже в этом разделе, относится к прошлому, второму
ревью.

**45 находок из 45 закрыты, остатков по ним нет.** Вопрос про
`registerSublogger`, который `M5` оставлял открытым, владелец решил
2026-08-19: люк остаётся, доказательство дороже. Подробности и замеры —
в «Решения, которые стоит знать», пункт 5.

Вопрос про обвязку буферизованных фасадов, который держался открытым по
той же причине (сведение меняет публичный дартдок на pub.dev), владелец
закрыл 2026-08-19: сводить. Сделано, см. раздел выше.

Небуферизованная пара, которая оставалась здесь незаданным вопросом,
сведена тем же приёмом по отдельному указанию владельца. Дублирования
обвязки в пакете больше нет: все четыре фасада стоят на двух приватных
базах.

`M10` прошлого ревью («очереди и буферы ничем не ограничены») закрыт
целиком — граница очереди и дефолтный `onDropped`, разделы выше. Он был
последней находкой с остатком; в записях ревью **до** четвёртого открытых
находок не осталось ни одной.

Открытых вопросов больше нет: 2026-08-19 владелец закрыл все три.
Умолчание `maxQueueSize` поднято (раздел выше), а порог SDK решено не
поднимать вообще — следствия этого решения в «Решениях, которые стоит
знать», пункты 6 и 7.

Из работы, которую можно взять, остался единственный пункт бэклога, и
владелец сказал 2026-08-19 не брать его сейчас. Бэклог не тронут.

## Волны правок

| Волна | Содержание | Коммит |
|---|---|---|
| 1 (P0) | `C1`, `H1`, `H3` (код), `H4`, `H5`, `M12`, `M19`, часть `L18` | `dc18db3` |
| 2 (P1) | `H2`, `M1`, `M2`, `M3`, `M16`, `M20`, `M21`, `M22` | `981ea09` |
| 3 (P1) | README: `M8`-`M11`, `M15`, часть `L17` | `b022b91` |
| 4 (P2) | `M4`-`M7`, `M13`, `M14` | `eca3042` |
| 5 (P2, P3) | `M17`, `M18`, `L1`-`L19` | `99b3d28` |
| 6 | инвариант про флаги связи (не находка) | `e42226b` |
| 7 | `unnecessary_ignore` выключен: его нет в SDK 3.6.0 | `af3bfe3` |
| 8 | вердикты приведены к форме проекта в обеих записях | `b2049e3`, `e4925d5` |
| 9 | порог SDK `^3.7.0` + переформатирование под 3.7 | `3385c58`, `6128bc8` |
| 10 | откат к `^3.6.0`, `meta` понижена до `^1.15.0` | `53de7c1` |
| 11 | Dependabot: checkout v7.0.1, ignore на мажоры `lints` | `cfc815b` |

Главное по существу:

- **`C1`** — адаптер `withParam()` реализует `Flushable`/`Closable`
  и делегирует владельцу общей очереди. Раньше `MultiPublisher.close()`
  молча терял всю очередь, докладывая об успехе.
- **`H1`** — счётная карта в `AsyncFormatterWithBufferAndParam` сравнивает
  лог по identity, как в непараметрическом двойнике.
- **`H2`, `M1`** — у `CustomLogger` появился публичный **`onError`**: единая
  точка для бросившего `transformer`, срабатывания guard'а и бросившего
  паблишера. Аддитивно — без обработчика поведение прежнее. Разрешается по
  цепочке родителей, а не копируется вниз: флага связи нет, `relink()` его
  не касается.
- **`M3`** — флаг guard'а публикации перенесён с логгера на уровневый
  логгер. Паблишер, логирующий в другой уровень того же логгера, больше не
  отвергается; цикл по-прежнему ловится.
- **`M15`** — в README появился подраздел «The full set»: таблица 2×2 со
  всеми восемью асинхронными классами, разбор `onError`/`sync`/`retryDelay`
  и блок про неограниченность очередей. До этого `AsyncFormatter*`,
  `retryDelay`, `Flushable`/`Closable`, `sync` и `isClosed` не встречались
  в README ни разу.
- **`M8`-`M11`** — `withAddedName`, которого нет в пакете, заменён на
  `child(...)`; заметка о том, что нужен не только уровень, но и паблишер;
  `StateError` после `close()` назван прямо; две семантики `flush` разведены.
- **`M4`, `M5`, `M6`** — жизненный цикл при закрытии: `close()` отменяет
  ожидающий ретрай вместо ожидания `retryDelay`, `flush()` во время закрытия
  возвращает future закрытия, а не завершённый, и появился **`onDropped`** —
  колбэк с записями, отброшенными при закрытии. До этого потеря была
  невидима: ни ошибки, ни счётчика.
- **`M7`** — правило «что достаётся новому уровню» вынесено в одну функцию,
  консультирующуюся с цепочкой родителей, и появился
  **`CustomLevelLogger.hasPublisher`**: способ отличить включённый уровень,
  который пишет в никуда, от рабочего.
- **`H5`** закрыт документированием, **`M2`** и **`M3`**-часть про
  асинхронные паблишеры — тоже (см. «Решения, которые стоит знать»).

Тестов 212 (было 170). Каждая кодовая правка проверена мутацией: откат
роняет соответствующий новый тест. Из четырёх мутаций, выживавших на момент
ревью, не осталось ни одной.

## Решения, которые стоит знать

1. **`H5`: бросивший `output` по-прежнему теряет батч — это контракт.**
   Симметрию с `format` я не делал: `format` безопасно ретраить целиком,
   потому что `output` при его отказе ещё не исполнялся, а к моменту отказа
   `output` сток мог уже принять часть батча — автоматический ретрай заменил
   бы тихую потерю тихим дублированием. Изменить это значит изменить
   контракт, а не только код.
2. **`H2`: дефолт `onError` оставлен прежним сознательно.** Тихо проглатывать
   ошибку, которую библиотека сама обнаружила, хуже, чем сообщить о ней.
   Исправлено не «перестало падать по умолчанию», а «появилось чем это
   выключить».
3. **`M2`: асинхронный паблишер защитить guard'ом нельзя в принципе** — его
   `handle` идёт после возврата из `publish`. Поэтому исправлено обещание,
   а не механика.
4. **`M7`: догадок про паблишер нового уровня я не делал.** Если корневой
   логгер настроен только пер-уровнево, новый уровень остаётся на no-op —
   выдать ему паблишер, выбранный для другого уровня, было бы выдумкой.
   Закрыта ненаблюдаемость (`hasPublisher`), а не «пусть хоть куда-то
   пишет».
5. **`M5`: `registerSublogger` остаётся расширяемой точкой — решение
   владельца от 2026-08-19.** Валидация `sublogger._parent == this`
   закрыла бы ловушку целиком, и цена ей замерена: восемь тестов, из них
   семь в группе «cycles in the sublogger graph». Они не удалились бы,
   а стали бы **ненаписуемыми** — закрыв люк, теряешь и возможность
   создать цикл, и возможность доказать, что распространение на нём
   завершается. Выбрано доказательство.

   Замерена и обратная сторона. Комментарий `custom_logger.dart:336-343`
   объявляет порядок присваивания флага load-bearing; при закрытом люке
   перенос присваивания в конец метода не ломает **ничего** сверх тех же
   восьми тестов. То есть ограничение на порядок в четырёх методах
   распространения существует ровно потому, что люк открыт. Это цена
   решения, а не случайность, и в комментарии это теперь сказано.

   Что люк не закрывает: логгер, зарегистрированный не через
   `CustomLogger.sub`, посчитан в `subLoggersCount`, не получает ничего
   и связать его нельзя никогда. Это описано в дартдоке и закреплено
   тестом — молчаливой ловушкой оно быть перестало.
6. **Порог SDK не поднимаем — решение владельца от 2026-08-19.**
   `^3.6.0` остаётся, job `oldest-supported` продолжает гонять настоящий
   Dart 3.6.0. Цена подъёма замерена заранее, а не предполагается: версия
   языка 3.7 переключает `dart format` на tall-style — переформатировались
   бы 37 файлов из 45, — а `require_trailing_commas` пришлось бы выключить.
   Подъём уже пробовали и откатывали (волны 9 и 10: `3385c58`, `6128bc8`,
   `53de7c1`).
7. **Два следствия шестого пункта, которые выглядят как недоделки.**
   `unnecessary_ignore` выключен в `analysis_options.yaml`: правила нет
   в SDK 3.6.0, а неизвестное имя правила там же становится
   `undefined_lint` и роняет job (`af3bfe3`); на настоящем Dart 3.7.0
   проверено, что правила нет и там. `lints 6.x` не берём: ему нужен
   `^3.8.0`, а мажоры `lints` вписаны в ignore Dependabot (`cfc815b`),
   так что новых PR по ним не придёт. Чинить не нужно ни то, ни другое:
   пока порог стоит, это и есть правильное состояние.

## Состояние проверок

Dart SDK 3.13.0 (stable), если не сказано иное:

| Проверка | Результат | Когда |
|---|---|---|
| `dart analyze` | 0 issues | `763766d` |
| `dart analyze --fatal-infos` | 0 issues | `763766d` |
| то же на **Dart 3.6.0** (пол SDK, `~/fvm/versions/3.27.0`) | 0 issues | `763766d` |
| то же на **Dart 3.7.0** (`~/fvm/versions/3.29.0`) | 0 issues | `763766d` |
| `dart test` | 342/342 зелёные | `763766d` |
| `dart test -p chrome` | 338/338 (четыре — `testOn: 'vm'`) | `763766d` |
| `dart test -p chrome -c dart2wasm` | 338/338 | `763766d` |
| `dart format --output=none --set-exit-if-changed .` | чисто | `763766d` |
| `dart doc` | Found 0 warnings and 0 errors | `763766d` |
| `dart pub publish --dry-run` | `logger_builder 0.7.0`, архив 89 КБ, **0 предупреждений** | `763766d` |
| чистый клон + `pub get` в корне и в `example/`, `dart analyze --fatal-infos` + `dart test` + dry-run | 0 issues, 342/342, 0 предупреждений (в 0.5.1 тут было exit 3 — `H3`) | `763766d` |
| то же на **Dart 3.7.0** (из Flutter 3.29) | 0 issues, 342/342 | `763766d` |
| Flutter 3.47 и 3.29: `pub get` + `flutter analyze` пробного пакета (саблогер, `maxRetries`, оба буферных базовых класса) | чисто | `2e25f36`, **не перепрогнано** |
| все десять точек входа в `example/` (пять в `bin/`, пять в `bin/async_publishers/`) | запускаются, exit 0, ни одной строки дефолтного репортёра | `763766d` |
| `dart test --test-randomize-ordering-seed` ×3 | 342/342 каждый раз | `763766d` |
| `dart compile js` / `dart compile wasm` | собираются | `763766d` |
| CI | все четыре job'а зелёные (`verify`, `oldest-supported`, `oldest-deps`, `beta`) | `763766d` |

Столбец «когда» читать буквально. `763766d` — последний коммит, тронувший
код, тесты или примеры; после него прогнано всё, кроме одной строки:
десять примеров, js и wasm, чистый клон на двух SDK, dry-run в клоне,
три случайных порядка тестов, анализ на трёх SDK и оба браузерных
прогона.

Не перепрогнана одна строка — **пробный Flutter-пакет**. Он живёт
в скретчпаде и рестарт не пережил, а обе волны публичной поверхности
не меняли: внутренние файлы, дартдок, тесты, CI, примеры и `Future.sync`
в теле `close()`. Ни одной сигнатуры, ни одного имени. Собрать заново
можно по инструкции в конце этого файла; CI при этом гоняет `oldest-deps`
и `oldest-supported`, то есть разрешение зависимостей на полу проверено
и там.

pana на `f03e962` давала 160/160 со всеми web-тегами; локально после
этого она не прогонялась, но CI гоняет проверку тегов pub.dev через pana
отдельным шагом — и теперь на запиненной версии 0.23.18 (`M5`).

Строка `CI` — прогон
[32319184440](https://github.com/vi-k/logger_builder/actions/runs/32319184440)
по `714755a`: все четыре job'а зелёные. Оговорка о `beta`: он помечен
`continue-on-error`, так что его «зелёный» — сигнал, а не гарантия.

Тестов 342 (было 333). Три добавила волна P0 — два регресс-теста на `H2`
и один на `H3`. Шесть добавила волна Medium и Low: буферизованный близнец
теста про бросающий `onDropped` (`M1`), два на мутации `M8`, один
контрактный на очередь из двух `flush()` и два на идентичность future
(`L9`, по одному на пайплайн). В браузере и под wasm идёт 338 из них:
четыре теста помечены `testOn: 'vm'`, потому что утверждают состоявшуюся
сборку мусора.

Архив 89 КБ: 82 было до границы очереди, 85 стало с ней, плюс два
килобайта дал репортёр с его дартдоком, плюс два — переписанные блоки
дартдока двух волн. Подъём умолчания размера не изменил. Состав архива
теперь зафиксирован в `.github/published_files.txt` и сверяется в CI.

Клоны прогнаны из настоящего `git clone` в скретчпад, на двух SDK:
текущем 3.13.0 и Dart 3.7.0 из Flutter 3.29. Оба дают 0 issues и
342/342. `dart analyze` в свежем клоне без `pub get` в `example/` даёт
483 issues — это неразрешённые зависимости примера, а не находка.

`dart doc` — 0 предупреждений, 0 ошибок. Раньше это требование держалось
на одной строке здесь и легко терялось; теперь оно в чеклисте релиза,
`docs/conventions.md` §7 пункт 1.

## Открытые вопросы

**Три, все от 2026-08-20 и все — последние находки четвёртого ревью.**
Работы в них нет, есть развилки, которые агенту не решить:

1. **`M2`: куда пишет дефолтный репортёр.** Сейчас `print`, то есть
   stdout приложения, а для CLI с `--json` или сервера поверх stdio это
   канал протокола. Прямой ответ «в stderr» стоит `dart:io` в `lib/`,
   а это три web-тега на pub.dev (160/160 → 130/160, проверено ревью).
   Значит, выбирать из трёх: условный импорт ради stderr на native,
   `dart:developer.log()` или оставить `print` и назвать канал в доках.
   Решать стоит **до** публикации 0.7.0: это умолчание, и менять его
   потом — снова ломающее изменение.
2. **`L6`: тега `v0.3.1` нет, а версия на pub.dev есть.** Проставить
   задним числом или оставить как есть — правка истории релизов.
3. **`L8`: покрытие не измеряется.** Вводить ли `--coverage` и порог —
   политика проекта.

См. «Четвёртое независимое ревью» и запись
`docs/records/2026-08-20[3]-medium-low-wave-report.md`.

Прежних открытых вопросов нет. Три, стоявшие здесь, закрыты решениями
владельца 2026-08-19:

1. **Умолчание `maxQueueSize`** — поднято с 10 000 до 100 000 (`2e25f36`).
   Раздел «Умолчание очереди — 100 000», запись
   `docs/records/2026-08-19[10]-max-queue-size-default-report.md`.
2. **Порог инструментария** и 3. **`lints 6.x`** — оба ждали подъёма
   порога SDK, а его решено не делать вовсе. `unnecessary_ignore`
   остаётся выключенным, `lints 6.x` — невзятым, и это больше не
   задолженность: подробности в «Решениях, которые стоит знать», пункты
   6 и 7.

## Что дальше

1. **Ответить на три вопроса из «Открытых вопросов».** Срочен из них
   один — `M2`: это умолчание, и после публикации оно правится только
   новым релизом. `L6` и `L8` не горят.
2. 0.7.0 подготовлен целиком и ждёт хода владельца — связку из пункта 6
   чеклиста запускает только отдельный запрос. Версия открывалась ради
   пер-уровневого пина паблишера (раздел «0.7.0: пин паблишера сделан»
   ниже), но несёт уже больше: пин, границу очереди `maxQueueSize`
   с политикой «отбрасывать входящий» и умолчанием 100 000, `onDropped`
   у небуферизованной четвёрки и его дефолтную реализацию, которая
   печатает потери. Три ломающих изменения из четырёх — про поведение
   под нагрузкой, и все помечены в `CHANGELOG.md`.
   **Уточнение от 2026-08-20:** все находки ревью, стоявшие в этом коде,
   закрыты, кроме `M2` — единственного оставшегося вопроса про
   умолчание, см. пункт 1.
3. В бэклоге один пункт, ломающий, не начат, и владелец 2026-08-19
   сказал не брать его сейчас.
   - **Саблогеры другого типа.** Работа крупная (потребует пересмотра
     генерик-связки), начинать с обсуждения и design-записи, а не с кода.

## 0.7.0: пин паблишера сделан

Пер-уровневый пин паблишера (`bda2635`) реализован целиком, все четыре
задачи плана закрыты. Дизайн —
`2026-08-18[1]-per-level-publisher-link-design.md` (`85482c7`, поправлен
дважды: `6050d0d`, `d97c96e`), план работ —
`2026-08-18[2]-per-level-publisher-link-plan.md` (`c32b19f`), оба
в `docs/records/` и оба согласованы с владельцем.

Модель: флагов два. Пин (`hasOwnPublisher`) живёт на уровне и ставится
только точечным присвоением; связь (`publisherLinked`) остаётся на
логгере со своим сегодняшним смыслом и гасится только общим сеттером.
Починка в том, что точечное присвоение связь больше не трогает.

Две правки дизайна пришли из попытки написать код, и обе стоит знать,
прежде чем предлагать «упростить»: одним флагом модель не собирается
(общему сеттеру пришлось бы пинить то, что он накрывает, и тогда
повторное `logger.publisher = A; logger.publisher = B;` перестаёт
работать), а пин не может служить маркером обхода вместо флага связи
(логгер вправе не иметь уровня, которым идёт распространение, — цикл
через него остался бы без защиты).

Четыре задачи плана закрыты по коммиту на каждую: пин и точечное
присвоение — `5d25516` (плюс `020206f` — перенос двух строк дартдока
под 80 колонок); общий сеттер против пина — `dd780dc`; уровневый
`relink()` — `a0db6f3`; документация и версия — `964e627`. Версия
в `pubspec.yaml` открыта как `0.7.0` обычным коммитом, не релизным,
а заголовок в `CHANGELOG.md` помечен `(unreleased)` — той же формой,
что стояла у 0.6.2 до него. Публикации не было, и связка «коммит —
тег — публикация» не разрешена: ни коммита `Release 0.7.0`, ни тега
`v0.7.0`, ни `dart pub publish` в репозитории нет и быть не должно, пока
владелец не запросит публикацию отдельным сообщением (`AGENTS.md` §5).

Финальное ревью работы разобрано отдельной волной. `3d512aa` — дефект
`I1`: уровневый `relink()`, которому нечего взять, рассылал вниз no-op
как значение, и у связанного саблогера `hasPublisher` оставался `true`
при паблишере, пишущем в никуда. Правка — рассылать сам сброс: аргумент
паблишера у `_inheritLevelPublisher` и `_propagateLevelPublisher` теперь
нуллабельный. Тем же коммитом — три теста на рассылку уровневого
`relink()`, которую раньше не покрывал ни один. `956e0d7` — оба README
(правило «саблогер отвязывается» больше не врёт про паблишеры),
`3de3424` — `docs/architecture.md` и `CHANGELOG.md`, `373eaa9` — эта
записка и отчёт волны. Отчёт с мутациями и вердиктами —
`docs/records/2026-08-19[1]-per-level-publisher-link-report.md`.

Ре-ревью волны нашло ещё четыре мелочи уже вне своей области, они закрыты
`b40bb06` и `da110f4`. Существенная из них одна: уровневый `relink()`
распространяется вниз так же, как сеттеры, но цикл-теста на этот путь
не было — группа покрывала только сеттеры. Теперь есть, и мутация это
подтверждает: уберёшь маркер обхода — тест падает. Остальные три были
неправдой в текстах: цепочка `_publisherFor` в `architecture.md`
обрывалась на шаг раньше (после родителей идёт собственный общий
паблишер логгера), отчёт волны обещал «по коммиту на задачу» над таблицей
из шести строк, а эта записка называла весь хвост до `4e57652` «всем
0.6.2», хотя туда же входят доковые коммиты 0.7.0.

## Если сессия оборвалась здесь

**Состояние на 2026-08-20.** Дерево чистое, всё запушено, коммитов
впереди нет. `HEAD` — документационный коммит волны Medium/Low;
последний коммит, тронувший код, тесты или примеры, — `763766d`.
CI зелёный на всех четырёх job'ах.

**Обе волны по четвёртому ревью закончены.** Двадцать находок из
двадцати трёх закрыты, вердикт стоит в конце каждой; проверено обходом.
Отчёты — `docs/records/2026-08-20[2]-p0-wave-report.md` (три P0) и
`docs/records/2026-08-20[3]-medium-low-wave-report.md` (семнадцать
Medium и Low). Проверки прогнаны заново на `763766d` целиком, кроме
пробного Flutter-пакета — таблица «Состояние проверок» и оговорка
под ней.

**Работы в полёте нет.** Следующий ход — за владельцем: три вопроса,
`M2`, `L6` и `L8`, см. «Открытые вопросы». Срочен из них только `M2`,
и то лишь потому, что 0.7.0 не выпущена.

**Чего НЕ делать при рестарте.** Не закрывать `M2`, `L6` и `L8` своей
властью: это не работа, а развилки, и каждая описана в «Открытых
вопросах» вместе с ценой вариантов. Не перепроверять то, что ревью уже
проверило и опровергло — список в разделе «Четвёртое независимое ревью».
Не перезапускать ревью целиком: запись полна, в ней 23 находки
с координатами, сценариями и доказательствами, у двадцати — вердикты.

**Три вещи, которые волны оставили после себя и которые легко нарушить:**

- **Состав архива зафиксирован** в `.github/published_files.txt`. Правка,
  меняющая набор публикуемых файлов, обязана обновить его тем же
  коммитом, иначе CI встанет на шаге «Validate the published archive».
- **CI гоняет тесты в браузере и под wasm.** Правка в `lib/`, которая
  может вести себя на web иначе, стоит локального `dart test -p chrome`.
  Четыре теста помечены `testOn: 'vm'` — не снимайте пометку, они
  утверждают состоявшуюся сборку мусора.
- **Анализатор с пола SDK строже текущего.** `testOn: 'vm'` без
  завершающей запятой прошёл локально на 3.13.0 и уронил все четыре
  job'а на 3.6.0 и 3.13.1. Для `test/` пол стоит гонять так же, как
  для `lib/`: `~/fvm/versions/3.27.0/bin/cache/dart-sdk/bin/dart analyze
  --fatal-infos`.

**Если три оставшиеся находки возьмут в работу** — по `AGENTS.md` §4
вердикт пишется отдельным абзацем в конце **своей** находки, прозу
находки не трогать, в зачине статус и координаты (волна и коммит).
В записи находки идут заголовками `## H1.`, `## M1.`, `## L1.` — форма
с номером в зачине вердикта (`**L1. Вердикт: …**`) здесь не нужна, она
была нужна в `2026-08-19[2]`, где Low оформлены абзацами. И проверяйте
результат обходом: в этой записи вердикт `L5` при первой попытке уехал
в конец `L6`, потому что между ними стоит находка без вердикта.

**0.7.0 подготовлена целиком технически.** Пункты 1-5 чеклиста
`docs/conventions.md` §7 пройдены, и таблица «Состояние проверок»
прогнана заново на `763766d`, последнем коммите с кодом, тестами или
примерами, — включая оба чистых клона (на 3.13.0 и на Dart 3.7.0), все
десять примеров, js и wasm, три случайных порядка тестов, анализ на трёх
SDK и оба браузерных прогона. Перепрогонять ничего не нужно;
единственная непрогнанная строка — пробный Flutter-пакет, и почему это
допустимо, сказано под таблицей.

**Все находки ревью, жившие в этом коде, закрыты, кроме `M2`** —
дефолтный репортёр пишет в stdout, а не в stderr, и после публикации это
правится только новым релизом. Ответ не очевиден: `stderr` тянет
`dart:io` в `lib/`, а это три web-тега на pub.dev. Развилка целиком —
в «Открытых вопросах».

**К владельцу три вопроса — `M2`, `L6`, `L8`.** Три прежних он закрыл
2026-08-19: умолчание `maxQueueSize` поднято до 100 000, порог SDK решено
не поднимать вовсе, пункт бэклога сейчас не брать.

По **прошлым** ревью не осталось ничего: закрыты второе независимое
ревью, финальное ревью пер-уровневого пина и третье независимое ревью —
все 45 находок, вердикт в конце каждой. Записи:
`docs/records/2026-08-17[1]-0.6.0-project-review.md`,
`docs/records/2026-08-19[1]-per-level-publisher-link-report.md`,
`docs/records/2026-08-19[2]-project-review.md`.
**Четвёртое ревью (`2026-08-20[1]`) разобрано почти целиком — двадцать
находок из двадцати трёх закрыты, открыты `M2`, `L6` и `L8`, и все три
ждут решения, а не работы.**

**Что было последним.** Волна Medium и Low по четвёртому ревью:
семнадцать находок, девять коммитов, шесть новых тестов, браузерный
и wasm-прогон в CI, отчёт `2026-08-20[3]`. До неё — волна P0: `H2`
(`7261a4e`), `H3` (`d469c3c`), `H1` (`714755a`), три новых теста, отчёт
`2026-08-20[2]`. До неё — само четвёртое независимое ревью на `f03e962`:
23 находки, ни одной правки в коде, запись `2026-08-20[1]` (`35c27d2`).
До него — умолчание `maxQueueSize` поднято с 10 000 до
100 000 по решению владельца — `2e25f36`, запись `2026-08-19[10]`,
раздел «Умолчание очереди — 100 000». До неё — дефолтная реализация
`onDropped`: пустой колбэк больше не значит тишину, пакет печатает
потери с окном (записи `2026-08-19[8]` и `[9]`). До неё — граница
очереди: `maxQueueSize` на всех
восьми паблишерах, `onDropped` у небуферизованной четвёрки, находка `M10`
закрыта целиком. Дизайн, план и отчёт — `docs/records/2026-08-19[5]`,
`[6]`, `[7]`; коротко — раздел «Граница очереди введена». Перед ней
сведена обвязка обеих пар фасадов (`4ce28ac`, `1150d6f`), отчёты
`2026-08-19[3]` и `[4]`.

**Бэклог не трогать** — прямое указание владельца от 2026-08-19; тогда
же он сказал не брать сейчас и сам пункт. В бэклоге он один, «Саблогеры
другого типа»: работа крупная и ломающая, начинать с обсуждения
и design-записи, а не с кода.

Публикации не было и связка «коммит — тег — публикация» **не разрешена**:
ни релизных коммитов, ни тегов `v0.6.2` и `v0.7.0` в репозитории нет.
`CHANGELOG.md` помечен `(unreleased)`, на pub.dev по-прежнему 0.6.1 —
опубликован и проверен. Разрешение должно прийти отдельным запросом
владельца (`AGENTS.md` §5).

Если `example/.../.dart_tool` не поднялся вместе с деревом — `dart pub get
--directory=example/logger_builder_examples`, `pubspec.lock` примера
в git не ходит.

Пробный Flutter-пакет из строки таблицы живёт в скретчпаде и переживает
рестарт не всегда — на `2e25f36` он собран заново и обе проверки прошли.
Собрать заново: `pubspec.yaml` с `flutter` из SDK и
`logger_builder` по `path` на чистый клон, копия
`example/logger_builder_examples/lib/simple_logger.dart` и файл, который
наследует оба буферных базовых класса и заводит саблоггер. Грабли одни:
саблоггер создаётся **конструктором** — в `Logger` нужно добавить
`Logger.sub(super.parent) : super.sub();`, метода `sub(...)` в пакете
нет.

Перед любым следующим действием, по `AGENTS.md` §1: сверь `git status
--short` и `git log --oneline -5` с этой запиской. Если расходится — верь
git и приведи этот файл в соответствие.

**Если рестарт когда-нибудь придётся на публикацию — сначала спроси
pub.dev, а не запускай `dart pub publish` заново.** Публикация
необратима, и повторный запуск на уже опубликованной версии — не «ещё
раз», а ошибка, после которой номер израсходован. Проверка:

```sh
curl -s https://pub.dev/api/packages/logger_builder | \
  python3 -c 'import json,sys; print(json.load(sys.stdin)["latest"]["version"])'
```

Версия на месте — публикация состоялась: допиши факт сюда и в запись
релиза, и ничего не запускай. Версии нет — не состоялась, и разрешение
нужно получать заново: оно даётся на одну публикацию и сгорает вместе
с сессией, см. `AGENTS.md` §5.

Рабочие материалы этой волны — реестр задач с тринадцатью принятыми
решениями, брифы, отчёты исполнителей и дифы ревью — лежат вне git,
в каталоге `.superpowers/sdd/` (вписан в `.git/info/exclude`), в папке
плана `2026-08-18[2]-per-level-publisher-link-plan`. Он переживёт
рестарт на этой машине, но не свежий клон; всё, что должно пережить
и клон, пересказано в
`docs/records/2026-08-19[1]-per-level-publisher-link-report.md`.
Удалять каталог до push'а не стоит: пока работа не уехала на origin, это
вторая карта отката помимо `git log`.

Чего в репозитории **нет** и не должно быть: временные пробы, которыми
проверялись находки ревью, жили в скретчпаде сессии вне репозитория и не
сохранились. Все доказательства пересказаны в вердиктах внутри записей
ревью — замеры, до/после, названия добавленных регресс-тестов и мутации,
которыми правки проверены. Воспроизводить пробы заново не нужно: то, что
они показывали, закреплено тестами в `test/`.
