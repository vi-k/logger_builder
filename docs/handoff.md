# Текущее состояние проекта

> Обновлено: 2026-08-16

Только текущее состояние. История — в `docs/records/`, пожелания
владельца — в `docs/backlog.md`, устройство пакета — в
`docs/architecture.md`.

## Где мы

В `main` подготовлен релиз **0.5.1**: версия в `pubspec.yaml` поднята,
`CHANGELOG.md` дополнен, `dart pub publish --dry-run` — 0
предупреждений. **На pub.dev не опубликовано** — публикацию делает
владелец руками. Тега `v0.5.1` тоже пока нет.

На pub.dev последняя версия — 0.5.0.

Что вошло в 0.5.1 (подробности —
`docs/records/2026-08-16[2]-transformer-reentrancy-report.md`):

- guard против реентерабельных трансформеров логов;
- README и примеры переведены на `publishLog` — до этого скопированный
  из них логгер молча игнорировал `CustomLogger.transformer`;
- четыре новых раздела README, закрывающие бэклог по нему
  (`docs/records/2026-08-16[3]-readme-sections-report.md`).

Отдельно, вне релиза, сделана первичная настройка под работу с агентами
(`AGENTS.md`, `docs/`) и наведён порядок в конфигурации анализатора
(`docs/records/2026-08-16[1]-analyzer-cleanup-report.md`).

## Состояние проверок

Dart SDK 3.13.0 (stable):

| Проверка | Результат |
|---|---|
| `dart analyze` | 0 issues |
| `dart test` | 145/145 зелёные |
| `dart format --output=none --set-exit-if-changed .` | чисто |
| `dart pub publish --dry-run` | 0 предупреждений |

## Открытые проблемы

**У волны 0.5.0 нет записи в `docs/records/`.** Что вошло в релиз, видно
по `CHANGELOG.md` и коммитам `313e3f0..89730fb`, но отчёта о волне и о её
финальном ревью не сохранилось.

## Что дальше

1. Владельцу: решить, публиковать ли 0.5.1. Дальше по чеклисту
   из `docs/conventions.md` — тег `v0.5.1`, push, `dart pub publish`.
2. Остаток бэклога — один пункт: саблогеры другого типа. Работа крупная
   и breaking (потребует пересмотра генерик-связки `CustomLogger<Logger,
   LevelLogger, LogFn, Log>` для гетерогенных иерархий), то есть 0.6.0.
   Начинать с обсуждения и design-записи, а не с кода.

Полезно знать при воспроизведении багов с рекурсией: `dart test` не
переживает падение изолята по Stack Overflow — виснет без вывода.
Запускать с редиректом в файл и внешним сторожевым таймером.
