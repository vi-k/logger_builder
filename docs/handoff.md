# Текущее состояние проекта

> Обновлено: 2026-08-17

Только текущее состояние. История — в `docs/records/`, пожелания
владельца — в `docs/backlog.md`, устройство пакета — в
`docs/architecture.md`, правила работы — в `AGENTS.md`.

## Где мы

Идёт разбор находок второго независимого ревью. Запись —
`docs/records/2026-08-17[1]-0.6.0-project-review.md`, проведено на коммите
`8a35e82` по уже исправленному коду: 1 Critical, 5 High, 22 Medium, 19 Low.
План работ с приоритетами — в конце той же записи, вердикты по закрытым
пунктам стоят отдельными абзацами внутри самих находок.

**Все находки закрыты** — пять волн, вердикт стоит в конце каждой находки.
Закрыт и последний пункт плана, не бывший находкой: инвариант «флаги связи
работают как маркеры обхода и тем ломают циклы в графе саблогеров» описан
в коде и в §7 `architecture.md` и закреплён тестами.

**Запушено.** `main` синхронен с `origin/main`, CI зелёный на обоих job'ах.

**Порог SDK — `^3.6.0`, `meta` — `^1.15.0`.** Решение владельца после
отката подъёма до `^3.7.0`. Смысл: заявленный порог совпадает с тем, что
реально проверяет job `oldest-supported` и с pubspec примера, а понижение
`meta` — то, что вообще делает пакет пригодным для Flutter. Flutter пинит
`meta` из своего SDK **точной** версией в старых релизах (3.24 и 3.27 — обе
1.15.0), поэтому `^1.16.0` там не разрешался, хотя Dart подходил. Пакету
ничего новее не нужно: используются только `@protected`,
`@visibleForTesting` и `@immutable`.

Первый пригодный Flutter стал 3.27 вместо 3.29. Проверено исполнением:
Flutter 3.29 и 3.47 разрешают пакет и `flutter analyze` пробного приложения
чист; потребитель с пином `meta 1.15.0` (как Flutter 3.24/3.27) теперь
разрешается, а раньше получал отказ солвера.

`require_trailing_commas` вернулся: он работает, пока версия языка ниже 3.7.
Если порог когда-нибудь пойдёт выше — правило придётся убрать, tall-style
удаляет ровно те запятые, которых оно требует (замерено: 43 замечания против
только что отформатированного дерева).

Тега `v0.6.0` нет,
последний тег — `v0.5.0`, на pub.dev тоже 0.5.0. Push и публикацию делает
владелец руками. Публикацию 0.6.0 разумно ставить после волны 4.

## Волны правок

| Волна | Содержание | Коммит |
|---|---|---|
| 1 (P0) | `C1`, `H1`, `H3` (код), `H4`, `H5`, `M12`, `M19`, часть `L18` | `dc18db3` |
| 2 (P1) | `H2`, `M1`, `M2`, `M3`, `M16`, `M20`, `M21`, `M22` | `981ea09` |
| 3 (P1) | README: `M8`-`M11`, `M15`, часть `L17` | `b022b91` |
| 4 (P2) | `M4`-`M7`, `M13`, `M14` | `eca3042` |
| 5 (P2, P3) | `M17`, `M18`, `L1`-`L19` | `99b3d28` |
| 6 | инвариант про флаги связи (не находка) | `e42226b` |
| 7 | `unnecessary_ignore` выключен: его нет в SDK 3.6.0 | `af3bfe3` |
| 8 | вердикты приведены к форме проекта в обеих записях | `b2049e3`, `e4925d5` |
| 9 | порог SDK `^3.7.0` + переформатирование под 3.7 | `3385c58`, `6128bc8` |
| 10 | откат к `^3.6.0`, `meta` понижена до `^1.15.0` | — |

Главное по существу:

- **`C1`** — адаптер `withParam()` реализует `Flushable`/`Closable`
  и делегирует владельцу общей очереди. Раньше `MultiPublisher.close()`
  молча терял всю очередь, докладывая об успехе.
- **`H1`** — счётная карта в `AsyncFormatterWithBufferAndParam` сравнивает
  лог по identity, как в непараметрическом двойнике.
- **`H2`, `M1`** — у `CustomLogger` появился публичный **`onError`**: единая
  точка для бросившего `transformer`, срабатывания guard'а и бросившего
  паблишера. Аддитивно — без обработчика поведение прежнее. Разрешается по
  цепочке родителей, а не копируется вниз: флага связи нет, `relink()` его
  не касается.
- **`M3`** — флаг guard'а публикации перенесён с логгера на уровневый
  логгер. Паблишер, логирующий в другой уровень того же логгера, больше не
  отвергается; цикл по-прежнему ловится.
- **`M15`** — в README появился подраздел «The full set»: таблица 2×2 со
  всеми восемью асинхронными классами, разбор `onError`/`sync`/`retryDelay`
  и блок про неограниченность очередей. До этого `AsyncFormatter*`,
  `retryDelay`, `Flushable`/`Closable`, `sync` и `isClosed` не встречались
  в README ни разу.
- **`M8`-`M11`** — `withAddedName`, которого нет в пакете, заменён на
  `child(...)`; заметка о том, что нужен не только уровень, но и паблишер;
  `StateError` после `close()` назван прямо; две семантики `flush` разведены.
- **`M4`, `M5`, `M6`** — жизненный цикл при закрытии: `close()` отменяет
  ожидающий ретрай вместо ожидания `retryDelay`, `flush()` во время закрытия
  возвращает future закрытия, а не завершённый, и появился **`onDropped`** —
  колбэк с записями, отброшенными при закрытии. До этого потеря была
  невидима: ни ошибки, ни счётчика.
- **`M7`** — правило «что достаётся новому уровню» вынесено в одну функцию,
  консультирующуюся с цепочкой родителей, и появился
  **`CustomLevelLogger.hasPublisher`**: способ отличить включённый уровень,
  который пишет в никуда, от рабочего.
- **`H5`** закрыт документированием, **`M2`** и **`M3`**-часть про
  асинхронные паблишеры — тоже (см. «Решения, которые стоит знать»).

Тестов 212 (было 170). Каждая кодовая правка проверена мутацией: откат
роняет соответствующий новый тест. Из четырёх мутаций, выживавших на момент
ревью, не осталось ни одной.

## Решения, которые стоит знать

1. **`H5`: бросивший `output` по-прежнему теряет батч — это контракт.**
   Симметрию с `format` я не делал: `format` безопасно ретраить целиком,
   потому что `output` при его отказе ещё не исполнялся, а к моменту отказа
   `output` сток мог уже принять часть батча — автоматический ретрай заменил
   бы тихую потерю тихим дублированием. Изменить это значит изменить
   контракт, а не только код.
2. **`H2`: дефолт `onError` оставлен прежним сознательно.** Тихо проглатывать
   ошибку, которую библиотека сама обнаружила, хуже, чем сообщить о ней.
   Исправлено не «перестало падать по умолчанию», а «появилось чем это
   выключить».
3. **`M2`: асинхронный паблишер защитить guard'ом нельзя в принципе** — его
   `handle` идёт после возврата из `publish`. Поэтому исправлено обещание,
   а не механика.
4. **`M7`: догадок про паблишер нового уровня я не делал.** Если корневой
   логгер настроен только пер-уровнево, новый уровень остаётся на no-op —
   выдать ему паблишер, выбранный для другого уровня, было бы выдумкой.
   Закрыта ненаблюдаемость (`hasPublisher`), а не «пусть хоть куда-то
   пишет».

## Состояние проверок

Dart SDK 3.13.0 (stable), прогнано перед этой записью:

| Проверка | Результат |
|---|---|
| `dart analyze --fatal-infos` | 0 issues |
| `dart test` | 212/212 зелёные |
| `dart format --output=none --set-exit-if-changed .` | чисто |
| `dart analyze` из чистого клона + `pub get` для `example/` | 0 issues (было exit 3 — `H3`) |
| то же на **Dart 3.7.0** (из Flutter 3.29) | 0 issues, 212/212 |
| Flutter 3.29 и 3.47: `pub get` + `flutter analyze` пробного приложения | чисто |

| `dart pub publish --dry-run` | 0 предупреждений (кроме «uncommitted») |
| все девять примеров | запускаются, exit 0 |
| `dart compile js` / `dart compile wasm` | собираются |

`dart doc` перед релизом прогнать заново.

## Открытые вопросы

1. **Порог инструментария держит `unnecessary_ignore` выключенным.** Job
   `oldest-supported` пинит SDK 3.6.0, где такого правила нет, а неизвестное
   имя правила там же становится `undefined_lint` и роняет job. Включить —
   когда поднимется порог (см. пункт 3). Это единственное, что осталось от
   первого прогона CI: `verify` прошёл целиком, включая все четыре новых
   шага (архив, примеры, js, wasm), `oldest-supported` упал только на этом
   правиле и после правки зелёный.
2. **`maxBufferSize` и политика сброса не вводились** (вердикт по `M10`
   прошлого ревью). Очереди неограниченны, это описано в dartdoc всех четырёх
   баз. Форму API стоит обсудить отдельно. Наблюдаемость потерь при закрытии
   закрыта в волне 4 через `onDropped`, но потерь **из-за переполнения**
   по-прежнему нет как понятия.
3. **`lints 6.x` заблокирован порогом.** Ему нужен `^3.8.0`, у нас `^3.6.0`,
   и job `oldest-supported` на 3.6.0 его не разрешит — оба PR Dependabot на
   `lints 6.1.0` упрутся в это. То же с `unnecessary_ignore`: проверено на
   настоящем Dart 3.7.0, что правила там ещё нет, значит и оно ждёт 3.8.
   Подъём порога — отдельное решение, и оно дороже, чем кажется: версия
   языка 3.7 переключает `dart format` на tall-style (37 файлов из 45), а
   `require_trailing_commas` придётся выключить.

4. **У волны 0.5.0 нет записи в `docs/records/`.** Что вошло, видно по
   `CHANGELOG.md` и коммитам `313e3f0..89730fb`.

## Открытые PR

Dependabot открыл три PR сразу после первого пуша, и все три показывают
`oldest-supported=FAILURE` **не по своей вине**: они ответвились от `main`
за две минуты до правки `af3bfe3`, которая убрала мой же `unnecessary_ignore`.
После ребейза эта причина исчезает.

| PR | Что | После ребейза |
|---|---|---|
| #1 | `actions/checkout` 5.1.0 → 7.0.1 | должен пройти: job `verify` на этой ветке уже зелёный, то есть сам action на раннере работает |
| #2, #3 | `lints` 5.1.1 → 6.1.0 | упадут по существу — см. открытый вопрос 3 |

## Что дальше

1. Владельцу: решить, публиковать ли 0.6.0. Дальше по чеклисту из
   `docs/conventions.md` — push, тег `v0.6.0`, `dart pub publish`.
2. Остаток бэклога — один пункт: саблогеры другого типа. Работа крупная и
   breaking (потребует пересмотра генерик-связки), начинать с обсуждения
   и design-записи, а не с кода.

## Если сессия оборвалась здесь

Все волны закоммичены, дерево после них чистое. По находкам ревью работы
не осталось — следующий шаг за владельцем, см. «Что дальше». Что уже сделано, видно по вердиктам в записи ревью — диффы
перечитывать не нужно.

Перед любым следующим действием, по `AGENTS.md` §1: сверь `git status
--short` и `git log --oneline -5` с этой запиской. Если расходится — верь
git и приведи этот файл в соответствие.

Чего в репозитории **нет** и не должно быть: временные пробы, которыми
проверялись находки ревью, жили в скретчпаде сессии вне репозитория и не
сохранились. Все доказательства пересказаны в вердиктах внутри записей
ревью — замеры, до/после, названия добавленных регресс-тестов и мутации,
которыми правки проверены. Воспроизводить пробы заново не нужно: то, что
они показывали, закреплено тестами в `test/`.
