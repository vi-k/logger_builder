# The examples follow the package's own rules. Keeping a full copy of the
# root config here would let the two drift apart, as it already had.
include: ../../analysis_options.yaml

linter:
  rules:
    # Example code documents itself by being an example.
    public_member_api_docs: false
