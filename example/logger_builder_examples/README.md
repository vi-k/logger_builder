Runnable examples for `logger_builder`: loggers in `lib/`, entrypoints in
`bin/`. Run any of them with `dart run bin/<name>.dart` — or
`dart run bin/async_publishers/<name>.dart` for the publisher examples, which
live in a subdirectory.
